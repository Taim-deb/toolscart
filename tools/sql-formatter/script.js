const $=s=>document.querySelector(s), T=window.ToolScart;const loadFile=async()=>{const f=$('#file').files[0]; if(!f) return; try { $('#in').value = await T.readFile(f,[".sql",".txt"]); $('#filemeta').textContent = 'Selected file: ' + f.name; T.setStatus('status','Loaded ' + f.name,'ok'); } catch (e) { T.setStatus('status', e.message, 'error'); } };if($('#file')) $('#file').addEventListener('change', loadFile); $('#process').onclick=()=>{const kw=['select','from','where','group by','order by','having','insert into','values','update','set','delete from','join','left join','right join','inner join','outer join','on','and','or','limit','offset']; let out=$('#in').value.trim().replace(/\s+/g,' '); kw.sort((a,b)=>b.length-a.length).forEach(k=>{const r=new RegExp('\\b'+k.replace(/ /g,'\\s+')+'\\b','ig'); out=out.replace(r,m=>'\n'+m.toUpperCase())}); $('#out').value=out.replace(/^\n/,'').replace(/\n+/g,'\n').trim(); T.setStatus('status','Formatted.','ok')}; $('#copy').onclick=()=>T.copy($('#out').value,'status'); $('#export').onclick=()=>T.download('query.sql',$('#out').value||$('#in').value); $('#clear').onclick=clearAll;